{
  "name": "live-video-streamer",
  "description": "Ultra low-latency live video streaming over CDN",
  "repository": "git@gitlab.com:spectral-ai/engineering/media/live-video-streamer.git",
  "author": "Spectral Compute",
  "license": "Custom",
  "private": true,
  "workspaces": [
    "client",
    "common",
    "demo-client",
    "server"
  ],
  "scripts": {
    "foreach": "(cd common && yarn $@) && (cd client && yarn $@) && (cd demo-client && yarn $@) && (cd server && yarn $@)"
  },
  "packageManager": "yarn@3.2.0",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.23.0",
    "@typescript-eslint/parser": "^5.23.0",
    "eslint": "^8.15.0",
    "ts-patch": "^2.0.1",
    "typescript": "^4.6.4"
  }
}
