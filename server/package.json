{
  "name": "live-video-streamer-server",
  "description": "Server for ultra low-latency live video streaming over CDN",
  "author": "Spectral Compute",
  "license": "Custom",
  "private": true,
  "main": "dist/live-video-streamer-server.js",
  "version": "0.0.0",
  "scripts": {
    "bundle": "ts-patch install && yarn webpack --mode production",
    "bundle-dev": "ts-patch install && yarn webpack --mode development",
    "clean": "rm -rf build dist",
    "lint": "yarn eslint --max-warnings=0 --ext .ts src",
    "start": "yarn bundle-dev && yarn node dist/live-video-streamer-server.js $@"
  },
  "dependencies": {
    "@babel/core": "^7.17.10",
    "@ckitching/typescript-is": "^0.1.21",
    "@types/node": "^17.0.34",
    "babel-loader": "^8.2.5",
    "http-status-codes": "^2.2.0",
    "ts-loader": "^9.3.0",
    "ts-patch": "^2.0.1",
    "typescript": "^4.6.4",
    "typescript-is": "^0.19.0",
    "webpack": "^5.72.1",
    "webpack-cli": "^4.9.2"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.23.0",
    "@typescript-eslint/parser": "^5.23.0",
    "eslint": "^8.15.0"
  }
}
